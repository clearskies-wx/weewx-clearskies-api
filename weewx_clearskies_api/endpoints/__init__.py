"""API endpoint routers.

Landing pad for Phase 2 Tasks 3–6: all /api/v1/* route handlers.
Each endpoint domain gets its own module (station.py, observations.py, etc.)
to keep files under ~500 lines per coding.md §3.
"""
